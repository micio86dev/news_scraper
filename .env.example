# OpenAI API Key for news categorization and summarization
OPENAI_API_KEY=sk-...

# MongoDB Connection String (Same DB as backend)
# For local development with Docker replica set:
DATABASE_URL="mongodb://localhost:27017/itjobhub?replicaSet=rs0&w=1&journal=true"

# Optional: explicit DB name (fallback if not included in DATABASE_URL)
MONGO_DB="itjobhub"

# Optional: RSS Feeds to scrape (comma separated)
RSS_FEEDS=https://example.com/feed.xml,https://another.com/rss
